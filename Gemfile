source "https://rubygems.org"

# This site is built and deployed via GitHub Actions (see .github/workflows/pages.yml),
# so we are NOT pinned to the legacy `github-pages` gem's Jekyll/Ruby versions.
gem "jekyll", "~> 4.4"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
